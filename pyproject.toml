[project]
name = "castforge"
version = "0.1.0"
description = "Open-source framework for automated podcast pipelines"
requires-python = ">=3.10"
dependencies = [
  "anyio>=4",
  "python-dotenv>=1",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
